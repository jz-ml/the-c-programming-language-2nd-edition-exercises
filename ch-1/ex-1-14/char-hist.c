#include <stdio.h>

#define ALPHABET_LENGTH 26

main()
{
    int i, c;
    int alpha_chars[ALPHABET_LENGTH];

    for (i = 0; i < ALPHABET_LENGTH; ++i) {
        alpha_chars[i] = 0;
    }

    while ((c = getchar()) != EOF) {
        
    }
}
